import os
from PIL import Image

assets_dir = r"d:\art_gallery\website\src\assets"
# Let's restore the original gallery_girl_2d.png first to get its clean mask.
# Wait, we can get it from git!
# Let's write a script to check if we can restore the file.
